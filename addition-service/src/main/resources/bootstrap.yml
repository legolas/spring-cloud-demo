# Port setting
server:
  port: 8085

spring:
  application:
    name: addition-service
  cloud:
    config:
      uri: http://localhost:8888

  boot:
    admin:
      client:
        url: http://localhost:9000
