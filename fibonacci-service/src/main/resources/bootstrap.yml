# Port setting
server:
  port: 8084

addition-service-url: http://localhost:8080

spring:
  application:
    name: fibonacci-service

  boot:
    admin:
      client:
        url: http://localhost:9000

  cloud:
    config:
      uri: http://localhost:8888
