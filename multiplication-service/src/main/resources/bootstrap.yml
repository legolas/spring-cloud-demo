# Port setting
server:
  port: 8082

spring:
  application:
    name: multiplication-service

  boot:
    admin:
      client:
        url: http://localhost:9000

  cloud:
    config:
      uri: http://localhost:8888
