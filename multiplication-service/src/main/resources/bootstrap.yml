# Port setting
server:
  port: 8082

spring:
  application:
    name: multiplication-service
  cloud:
    config:
      uri: http://localhost:8888
  boot:
    admin:
      client:
        url: http://localhost:9000
