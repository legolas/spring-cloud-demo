# Port setting
server:
  port: 8083

spring:
  application:
    name: division-service

  boot:
    admin:
      client:
        url: http://localhost:9000

  cloud:
    config:
      uri: http://localhost:8888
