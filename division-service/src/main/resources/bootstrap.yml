# Port setting
server:
  port: 8083

spring:
  application:
    name: division-service
  cloud:
    config:
      uri: http://localhost:8888
  boot:
    admin:
      client:
        url: http://localhost:9000
